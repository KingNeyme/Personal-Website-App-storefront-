{
  "name": "caribai-public-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync:static": "node scripts/sync-static.mjs",
    "dev": "npm run sync:static && next dev",
    "build": "npm run sync:static && next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}
